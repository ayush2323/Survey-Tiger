import React from 'react'

const Publish = (props) => {
    return (
        <div>
            <h1>publish</h1>
            {console.log("props")}
        </div>
    )
}

export default Publish
